import React from "react";

const Home = () => {
  return (
    <div>
      <button to="/login">Login</button>
      <button to="/createaccount">Create Account</button>
    </div>
  );
};

export default Home;
